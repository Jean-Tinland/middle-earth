{
  "name": "middle-earth",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --open=./index.html",
    "generate-tiles": "bun scripts/generate-tiles.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sharp": "^0.34.5"
  }
}
